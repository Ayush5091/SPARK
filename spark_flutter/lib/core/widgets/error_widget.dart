import 'package:flutter/material.dart';

// TODO: Implement lib/core/widgets/error_widget.dart
class ErrorWidget extends StatelessWidget {
  const ErrorWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('ErrorWidget')), body: const Center(child: Text('ErrorWidget')));
  }
}
