import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/activity/submit_proof_screen.dart
class SubmitProofScreen extends StatelessWidget {
  const SubmitProofScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('SubmitProofScreen')), body: const Center(child: Text('SubmitProofScreen')));
  }
}
