import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/activity/request_activity_screen.dart
class RequestActivityScreen extends StatelessWidget {
  const RequestActivityScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('RequestActivityScreen')), body: const Center(child: Text('RequestActivityScreen')));
  }
}
