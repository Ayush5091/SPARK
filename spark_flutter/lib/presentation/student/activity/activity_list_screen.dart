import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/activity/activity_list_screen.dart
class ActivityListScreen extends StatelessWidget {
  const ActivityListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('ActivityListScreen')), body: const Center(child: Text('ActivityListScreen')));
  }
}
