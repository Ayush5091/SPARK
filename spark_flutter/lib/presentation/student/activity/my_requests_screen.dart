import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/activity/my_requests_screen.dart
class MyRequestsScreen extends StatelessWidget {
  const MyRequestsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('MyRequestsScreen')), body: const Center(child: Text('MyRequestsScreen')));
  }
}
