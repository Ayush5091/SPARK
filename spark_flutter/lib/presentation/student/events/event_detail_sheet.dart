import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/events/event_detail_sheet.dart
class EventDetailSheet extends StatelessWidget {
  const EventDetailSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('EventDetailSheet')), body: const Center(child: Text('EventDetailSheet')));
  }
}
