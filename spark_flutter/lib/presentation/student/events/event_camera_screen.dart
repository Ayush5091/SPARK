import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/events/event_camera_screen.dart
class EventCameraScreen extends StatelessWidget {
  const EventCameraScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('EventCameraScreen')), body: const Center(child: Text('EventCameraScreen')));
  }
}
