import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/profile/student_profile_screen.dart
class StudentProfileScreen extends StatelessWidget {
  const StudentProfileScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('StudentProfileScreen')), body: const Center(child: Text('StudentProfileScreen')));
  }
}
