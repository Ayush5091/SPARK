import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/student/profile/student_dashboard_screen.dart
class StudentDashboardScreen extends StatelessWidget {
  const StudentDashboardScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('StudentDashboardScreen')), body: const Center(child: Text('StudentDashboardScreen')));
  }
}
