import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/shared/main_admin_shell.dart
class MainAdminShell extends StatelessWidget {
  const MainAdminShell({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('MainAdminShell')), body: const Center(child: Text('MainAdminShell')));
  }
}
