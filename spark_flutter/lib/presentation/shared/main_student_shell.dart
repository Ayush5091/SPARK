import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/shared/main_student_shell.dart
class MainStudentShell extends StatelessWidget {
  const MainStudentShell({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('MainStudentShell')), body: const Center(child: Text('MainStudentShell')));
  }
}
