import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/auth/admin_login_screen.dart
class AdminLoginScreen extends StatelessWidget {
  const AdminLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('AdminLoginScreen')), body: const Center(child: Text('AdminLoginScreen')));
  }
}
