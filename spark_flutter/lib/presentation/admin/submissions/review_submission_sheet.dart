import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/admin/submissions/review_submission_sheet.dart
class ReviewSubmissionSheet extends StatelessWidget {
  const ReviewSubmissionSheet({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('ReviewSubmissionSheet')), body: const Center(child: Text('ReviewSubmissionSheet')));
  }
}
