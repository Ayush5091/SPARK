import 'package:flutter/material.dart';

// TODO: Implement lib/presentation/admin/events/admin_events_screen.dart
class AdminEventsScreen extends StatelessWidget {
  const AdminEventsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('AdminEventsScreen')), body: const Center(child: Text('AdminEventsScreen')));
  }
}
